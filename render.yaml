services:
  - type: web
    name: discord-welcome-bot
    env: python
    plan: free
    buildCommand: ""
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
